# Copy to .env.local and set your key. Used by /api/soc-copilot for LLM triage.
OPENAI_API_KEY=

# SQLite database for cases/audit/reports (Prisma)
DATABASE_URL="file:./dev.db"

# Optional: shared secret for webhook ingestion route
INGEST_WEBHOOK_SECRET=
